function togglePanel(i) { const panel = document.getElementById("panel" + i); panel.style.display = panel.style.display === "block" ? "none" : "block"; }

function submitForm() { alert("Thanks! Your booking request has been submitted. We'll contact you within 1 hour."); }
